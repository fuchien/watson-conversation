import { CarregarDirective } from './carregar.directive';

describe('CarregarDirective', () => {
  it('should create an instance', () => {
    const directive = new CarregarDirective();
    expect(directive).toBeTruthy();
  });
});
